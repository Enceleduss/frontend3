let isRegistered = false;

/**
 * This function "scans" your registry and POSTs it to Java.
 * It uses a module-level flag to ensure it only runs once per server startup.
 */
export const registerExpressionsWithJava = () => {
  if (isRegistered) return;

  
}